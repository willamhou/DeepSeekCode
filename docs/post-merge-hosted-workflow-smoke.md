# Post-Merge Hosted Workflow Smoke

This temporary fixture proves the DeepSeekCode hosted write workflow still works
after the hosted workflow bridge fixes were merged into the default branch.

Requested state: after
Current state: after
